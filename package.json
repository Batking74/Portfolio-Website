{
  "name": "portfolio-website",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"npm run client\" \"npm run devSever\"",
    "client": "cd client && npm start",
    "devSever": "cd server && npm run dev",
    "server": "cd server && npm start",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
